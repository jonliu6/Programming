application.title=File Upload Demo

# File Upload Parameters
file.upload.location=${file.upload.location}
file.upload.size.threshold=${file.upload.size.threshold}
file.upload.size.max=${file.upload.size.max}
file.upload.request.size.max=${file.upload.request.size.max}
