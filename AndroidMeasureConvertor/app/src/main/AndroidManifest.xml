<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.freecode.demo.android.measureconverter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TemperatureActivity"
            android:label="@string/miTemperature" />
        <activity
            android:name=".AreaActivity"
            android:label="@string/miArea" />
        <activity
            android:name=".LengthActivity"
            android:label="@string/miLength" />
        <activity
            android:name=".WeightActivity"
            android:label="@string/miWeight" />
        <activity
            android:name=".VolumeActivity"
            android:label="@string/miVolume" />

    </application>

</manifest>